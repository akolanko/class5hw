<h3><%= @post.title %></h3>

<span class="postbody"><%= @post.body %></span>
<br><br>
<span class="posttime"><%= @post.created_at %></span> <%= link_to "[Delete]", user_post_path(@post.user, @post.id), method: :delete, confirm: "Are you sure you want to delete this post?" %> <%= link_to "[Edit]", edit_user_post_path(@post.user, @post) %>
<br><br><br>
<%= link_to "[Back to Posts]", user_posts_path(@user.id) %>