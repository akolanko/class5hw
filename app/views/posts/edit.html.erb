<h3>Edit Post</h3>

<div id="form">

	<%= form_for [@post.user, @post] do |f| %>
		<%= f.label :title, "Title:" %>
		<br>
		<%= f.text_field :title %>
		<br>
		<%= f.label :body, "Content:" %>
		<br>
		<%= f.text_area :body %>
		<br>
		<%= f.submit "Update", :class => "submit" %>
	<% end %>

</div>